import type { EdgeTypes, NodeTypes } from '@xyflow/svelte';
/** Node type registry for all BPMN shapes. */
export declare const bpmnNodeTypes: NodeTypes;
/** Edge type registry for all BPMN connecting objects. */
export declare const bpmnEdgeTypes: EdgeTypes;
