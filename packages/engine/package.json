{
  "name": "@bsf/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The bpmn-svelte-flow (BSF) browser BPMN engine: executes standard BPMN 2.0 semantics with JavaScript (text/javascript) scripts, mocks, conditions and embedded tests over a bpmn-moddle tree. Zero runtime dependencies; runs in the browser and in bun.",
  "exports": {
    ".": {
      "types": "./src/engine.d.mts",
      "default": "./src/engine.mjs"
    },
    "./moddle": {
      "types": "./src/bsf-moddle.d.ts",
      "default": "./src/bsf-moddle.js"
    },
    "./adapter": "./src/bpmn-engine-adapter.mjs"
  },
  "scripts": {
    "test": "bun test/engine.test.mjs && bun test/conformance.test.mjs"
  },
  "devDependencies": {
    "bpmn-engine": "^25.0.1",
    "bpmn-moddle": "^10.2.0",
    "feelin": "^7.0.1"
  }
}
